

<div class="row-fluid">
    <div class="span4 ">
        <div class="yoddle_show">
            <!-- may need medium in the image tag -->
         
            <p>
               <%= image_tag @yoddle.image %>
            </p>
            <p>
       <strong>
            Title: <%= @yoddle.title %>
        </strong>  
    
            <p>
              <%= @yoddle.content.html_safe %>
            </p>
            <p>
       <strong>
           <h6> Yoddled Pic by <%= link_to @yoddle.user.name %></h6>
        </strong> 
    </p> 
<p>
              <%= link_to nil,"#{@yoddle.url}"  %>
            </p>
             <%= social_share_button_tag(@yoddle.title, :'data-source' => "https://raw.github.com/vkulpa/social-share-button/master/lib/assets/images/sprites/social-share-button/tumblr.png", :'data-type' => 'photo') %>
            <% if current_user == @yoddle.user %>
            
            <%= link_to 'Edit', edit_yoddle_path(@yoddle) %> |
            <%= link_to 'Destroy', @yoddle, method: :delete, data: { confirm: 'Are you sure?' } %> |
            <% end %>
      <%= link_to 'Back', yoddles_path %> <br />
            <P>
        <%= link_to "Yoddlem ⓒ", root_path, class:"brand" %>
    </P>
        </div>
    </div>


<div class="row-fluid">
<div class="yoddle_Comment">
    <div class="span6 ">
     <h4>Comments</h4>
<%= render "comments/comments" %>
<Aside>
<h6>New Comment</h6>
<%= form_for [@commentable, @comment] do |f| %>
  <% if @comment.errors.any? %>
    <div class="error_messages">
      <h2>Please correct the following errors.</h2>
      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
     </div>
  <% end %>
  <div class="field">
    <%= f.text_area :content, rows: 8 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
  </div>
  </div>
  </div>